{% set navInfo = [
    {
        id: '/',
        title: 'About'
    }, {
        id: '/research',
        title: 'Research'
    }
] %}

<ul class="nav-links">
    {% for info in navInfo %}
        <li>
            <a href="{{ info.id | url}}" class="{{ 'active' if info.id == navCategory }}" aria-current="{{ 'page' if info.id == navCategory else 'false'}}">{{info.title}}</a>
        </li>
    {% endfor %}
    <li>
        <a href="{{ '/assets/pdfs/ColinLefevre_CV.pdf'  | url }}" target="_blank" rel="noopener noreferrer" aria-label="Curriculum Vitae, opens PDF in new tab">CV</a>
    </li>
</ul>